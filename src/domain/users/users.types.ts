export declare namespace Body {
  interface signup {
    email: string;
    password: string;
  }
}

export declare namespace Payload {
  interface signup {
    email: string;
    password: string;
  }
}
